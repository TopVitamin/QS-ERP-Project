# 强盛科技自研 ERP 项目 · 协作须知

虚拟企业「强盛科技」的供应链 ERP 产品设计项目：文档在 `01–04`，可交互原型在 `09-产品原型`。

## 阅读与权威顺序

- 按 `README.md` 的层级读，不跳层：01 背景 → 02 模块规则 → 03 业务设计 → 04 字段与 PRD → 09 原型。
- 冲突时：背景看 01，模块规则看 02，业务设计看 03，字段与 PRD 看 04，页面行为以 09 代码为准。
- 越靠近实现的文档越有解释权：系统表达、状态取值以 04 和 PRD 为准；业务规则仍以 03 为准；01 里的系统设计文档（如单据分层设计）属模型说明，其中的枚举和流程是示例。
- `00-草稿对话` 是故事草稿，不作为范围、规则或字段依据；与 01–04 冲突时以 01–04 为准。
- 标「待确认」的内容不是最终方案，不得当作结论引用或据此实现。

## 写作与改动纪律

- 用中文；术语沿用 01/02 的现有叫法，不自造同义词。
- 不编造业务规则、数据和接口行为；没有依据就标「待确认」，不靠假设补全。
- 不为迁就原型去改 01–04 的结论，也不拿「待确认」项去补原型行为。
- 01–04 是文档交付物，09 是演示原型；不要提交 `node_modules`、`dist` 等产物。
- 目录级规则见 `02-需求调研和分析/AGENTS.md`（写需求文档前读）、`03-业务分析和设计/AGENTS.md`（写业务设计前读）、`04-产品设计方案/AGENTS.md`（写产品设计文档前读）和 `09-产品原型/AGENTS.md`（改原型前读）。

## 面向人的可读性（01–04 通用）

01–04 既要给 AI 读，也要给人读，写成能直接读顺的中文。

- 精简、通俗优先：删冗余，拆长句，把事讲清楚，不堆术语。
- 避免 AI 味：表格单元格里不用“触发：…。前置：…”这类电报式标签；不堆“本域／本稿／口径／承接／闭环”；不整篇“不是 X，而是 Y”。
- 文档之间引用写完整文件名（如《采购业务管理需求分析》），不写“01/4”“01/5”“采购分析”这类简写；FR／AC 编号可以保留，但要能看出属于哪份文档。
- 系统内部说法不进业务设计层：“下推”“回写”“状态枚举”“执行标识”换成业务说法。
- 更新或查阅 01–04 文档时，顺带润色：删冗余、拆长句、统一术语、简写换全称。

## 交付与评审纪律

- 同一件事尽量只有一个权威位置，但不强求唯一：为了说明、沟通和补充，允许同一事实出现在多份文档里。条件是——指明谁是 owner、副本标注“说明／示例”、写明不一致时听谁的（**越靠近实现越有解释权**）。改结论时同步检查引用它的地方。
- 重要文档写完跑一轮独立评审，分三线、只读不改：事实核对（结论能不能指到上游编号）、完整性（按自检清单逐条打钩）、语言（AI 味、长句、内部术语、引用简写）。输出格式统一：文件 + 行号 + 原文 + 问题类型 + 依据 + 严重度 + 建议。
- 评审意见先逐条对原文再采纳（常见偏差：引用旧版本、与正文重复、把行业惯例当成本项目规则），分「会影响对错」和「文档卫生」处理，不采纳的写明理由。
