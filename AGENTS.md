# 强盛科技自研 ERP 项目 · 协作须知

虚拟企业「强盛科技」的供应链 ERP 产品设计项目：文档在 `01–03`，可交互原型在 `09-产品原型`。

## 阅读与权威顺序

- 按 `README.md` 的层级读，不跳层：01 背景 → 02 模块规则 → 03 字段与 PRD → 09 原型。
- 冲突时：背景看 01，模块规则看 02，字段看 03，页面行为以 09 代码为准。
- `00-草稿对话` 是故事草稿，不作为范围、规则或字段依据；与 01–03 冲突时以 01–03 为准。
- 标「待确认」的内容不是最终方案，不得当作结论引用或据此实现。

## 写作与改动纪律

- 用中文；术语沿用 01/02 的现有叫法，不自造同义词。
- 不编造业务规则、数据和接口行为；没有依据就标「待确认」，不靠假设补全。
- 不为迁就原型去改 01–03 的结论，也不拿「待确认」项去补原型行为。
- 01–03 是文档交付物，09 是演示原型；不要提交 `node_modules`、`dist` 等产物。
- 目录级规则见 `02-需求调研和分析/AGENTS.md`（写需求文档前读）、`03-产品设计方案/AGENTS.md`（写设计文档前读）和 `09-产品原型/AGENTS.md`（改原型前读）。
