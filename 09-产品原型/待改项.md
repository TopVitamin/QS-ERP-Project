# 原型待改项

记录演示原型与已确认口径不一致的地方，改原型时按这份清单处理。

- 口径依据在 01–03：业务规则看 03《业务分析和设计》，需求范围看 02。字段明细见 `04-产品设计方案/20260812-ERP项目第一版框架搭建/字段清单合集/`。
- 这里只列差异，不重复规则内容；条目处理完就删掉，不留历史。
- 原型内部的页面规则、组件和颜色仍看 `原型开发说明.md`；开工与交付验证要求看 `AGENTS.md`。

## 模块未开放（做到对应页再改）

- 系统集成中心（业财结果单据／推送异常／人工补充）还没接入；入库金蝶失败的正式重推入口等该模块做出来再接。
- 工作台内容仍是演示页，一期不做；侧栏保留入口，默认落地采购订单。
- 销售（独立站订单、外部电商订单）与系统集成等菜单页尚未开放，已拍板口径等做到对应页再改。基础资料、价格管理（2026-09-23）、库存管理（2026-09-24）已开放；旧入口 `warehouse-list`、`base-logistics` 仅作 hash 兼容映射。
- 系统设置-用户与权限（`settings-users`）未开放，等系统管理PRD出来再做。

## 基础资料核验（2026-09-23）

- 商品编码系统生成入口未实现：全局规则允许系统生成或手工录入，商品新增页目前只能手工录入；种子数据已按 `SP+三级分类代码+4位序号` 命名（2026-09-24），生成入口与格式评审后再做。
- 商品保存未生成初始价目记录：价格管理已开放（2026-09-23），价目表演示数据按商品建档字段（初始供应商／初始含税价／币别／商品税务信息）推导模拟；正式的商品建档生成初始价按《商品资料主PRD》F02、AC05 实现后再接。
- 客户/供应商地址明细缺"国家／地区、邮政编码、地址备注"（表单与详情），字段清单标"新增/编辑/详情=是"；待补齐三列。
- 客户地址类型选项与种子不一致（选项为办公/发货/退货，种子为"收货"），字典待04确认。
- 客户等级展示为"A级"等，客户字段清单与Demo写"S、A、B、C"；待04统一写法后同步。
- 辅助资料编码序号在删除后会重用（全局规则要求"删除后的编码不重用"）；待记录已用序号后再分配。
- 对接授权配置（仓库）现为文本输入，Q03确认后改下拉并接入选项来源。
- 历史单据名称快照：主数据PRD已明确"建单保存时固化"，销售/采购对应单据字段清单需补快照字段与取值时点（本轮未做）。
- 商品列表多条码暂不支持展开、详情图片暂不支持放大；商品表单金额/税率精度校验待补（低优先）。
- 自动关单演示数据：销售订单、采购订单自动关闭时，关闭原因与关闭操作人应为空、用关闭方式表达（01《编码规则》与各PRD）；原型 Mock 仍写「全部发货自动关单」「系统」，对齐时一并改。
- 通知单字段集差异（采退发货通知、销退收货通知含「最近处理时间」，采购收货通知、销售发货通知不含），待确认是否有意为之，04核对后统一。

### 采购订单（`purchase-order`）

- 列表查询区增加「承诺交期」日期范围筛选项（主PRD、字段清单已定）。
- 列表「收货仓库」示例值用逻辑仓前缀（`LWH…`），不用 `WH…`。
- 移除「单据日期」字段及查询区、列表列、表单、详情对应位置，时间口径改用创建时间（2026-09-22确认；主PRD V0.14、字段清单已删行）。

### 退货链路（采购退货单、销售退货单及对应通知、结果单）

- 退货链路六个单据（采购退货单／采退发货通知单／采退出库单、销售退货单／销退收货通知单／销退入库单）已按 04 的 PRD 套件接入，含草稿删除与正式列表导入；导入为按「单据序号」分组、任一行失败整批不导入的正式能力。
- 退货截止日期按天粒度：表单与「调整退货截止日期」弹窗用 `FormControl` 的 `date` 类型（`DatePicker`），列表、详情与导入模板都按 `YYYY-MM-DD`；存量数据带时间的按日期截断展示（2026-09-23确认）。
- 分组导入的 Toast 走 `lib/feedback.js`，未接 `ImportExportActions` 的通用「导入完成：新增 N 条」文案（PRD 要求「成功导入N张退货单草稿，请逐单检查后提交审核」）。

### 价格管理（采购价目表、销售价目表、采购价格调整单、销售价格调整单）

- 四个对象已按 04 的 PRD 套件接入（2026-09-23）：价目表只查看／查询＋导出；调整单含草稿保存、提交、审核（审核通过后本地 Mock 更新价目表当前价）、驳回、删除与双向追溯；已驳回单保存后回到草稿（2026-09-24确认）。
- 供应商／客户筛选项按 PRD 取「审核通过且启用」；价目表与调整单的演示数据含已停用或待审核供应商的历史记录（如 `SUP000003`），这类供应商不可在筛选中选择，从调整单「查看价目表」跨页预置该供应商时筛选控件不回显（筛选本身生效）。同类现象在采购订单等模块的种子数据已存在。

### 全局

- 详情页在无行上下文（直接打开详情 hash）时按空值渲染，未实现「单据不存在／加载失败」错误态；现有各模块详情页同一行为，PRD 要求提供返回列表的错误态。
- 「离开未保存」弹窗沿用框架统一文案（标题「确认离开此页面？」、正文「当前修改尚未保存，离开后内容将丢失。」、取消按钮「继续编辑」）；各模块 Demo PRD 写的是「离开当前页面？」「离开后未保存的内容将丢失。」「取消」，统一改框架文案时一次对齐。
- 导出成功 Toast 由 `ImportExportActions` 统一提示「导出任务已创建：{任务号}」；各模块 Demo PRD 写的是「导出任务已创建，请到导入导出中心查看和下载」，统一改公共组件文案时一次对齐。
- 运行时时间戳（创建时间、最后更新时间、审核时间等）用 `nowStamp()`（`new Date().toISOString()`，UTC）写入，与种子数据和「北京时间」规则差 8 小时；各模块同一实现，统一改为北京时间时一次对齐。
- Demo PRD 里的失败类兜底文案（「搜索失败，请检查查询条件后重试」「您暂无{动作}权限，请联系管理员」「操作失败，请稍后重试」等）原型未实现：没有真实接口失败分支和权限模型，各模块一致；接后端与权限后统一补。
- 演示数据持久化在 `localStorage`，种子更新不会覆盖浏览器里已有的旧数据（各模块 Demo PRD 提到的「重置演示数据后恢复初始值」尚无入口）；换一批演示数据或改种子后需要手动清空对应 key 或加重置入口。
- 查询区「重置」会保留「审核状态」的已选值（`useListPageState.resetFilters` 为审核页签保留该维度）；对把审核状态做成普通筛选项、没有审核页签的列表（如采购入库单、其他出入库结果单、直接调拨单），重置后审核状态不会清空，需要再手动清除或改共享 hook 的保留条件；各模块同一行为，统一改时一次对齐。

### 结果单（采购入库单／销售出库单）

- 列表展示价税合计、税额、创建时间、最后更新时间（字段清单已改为「是」）。
- 自动生成的结果单，创建人、最后更新人显示「系统」。

### 库存查询

- 已实现（2026-09-24）：冻结／解冻行内入口与弹窗、每次冻结／解冻生成一条库存流水（事件类型「冻结」「解冻」，来源单号为空，记录操作人与时间）、预占「查看来源」、行内「查看流水」按当前筛选条件跳转、0库存行勾选框。
- 虚拟在途仓档案口径待定（主数据在途档案设计定稿前）：原型以普通逻辑仓 `LWH000009 深圳在途仓` 承载在途记账（`warehouseKind: 'transit'`），在途仓行按普通行统计与操作；不能选为业务单据出入库仓的限制只在业务单据选仓时生效（库存查询主PRD Q04）。
- 库存演示补充了逻辑仓种子 `LWH000005`～`LWH000009`（同实体仓同库存状态第二仓、待检品仓、已禁用仓、在途仓），用于满足库存查询与库存流水的演示覆盖要求；正式主数据以基础资料模块为准。
- 预占来源弹层列出的是「来源单据类型＋来源单号＋行号＋剩余预占数量」；演示种子中部分预占记录由库存模块自行 Mock，结果单侧（其他出入库、调拨）产生的预占由各模块审核动作写入同一集合。

### 库存流水

- 已实现（2026-09-24）：查询区「变动时间」用 `datetime-range`（`DateTimeRangePicker` + `matchesDateTimeRange`）；列表按事件类型＋四组变动明细展示，预占组反映消耗与释放；来源单号可点击进入结果单详情，结果单不存在时提示「结果单不存在或不可访问」并停留列表页。
- 采购／销售／采退／销退结果单按其模块的 Mock 集合按单号定位；演示种子中若该结果单不在对应模块，点击会按 PRD 提示不存在，属预期行为。
- 库存流水按只读记录实现，新增、编辑、删除、导入均不提供；导出按导出中心。

### 库存比对

- 已实现（2026-09-24）：只读列表、按实体仓／商品／库存状态／差异方向／比对日期查询、默认展示最近一次比对结果、导出；不提供新增、编辑、删除、导入、补跑与差异处置入口。
- 单边数据（主PRD Q03 待确认）不构造缺失行；仓库数量与快照时间取演示种子，快照取数口径待接口设计（Q01、Q02）。
- 差异方向的语义色：无差异用默认文字色，ERP多／仓库多按语义色 `warning`（Demo PRD 口径）。

### 其他出入库与调拨（2026-09-24）

- 已实现：其他入库申请单／其他入库单、其他出库申请单／其他出库单、分步式调拨单、调出通知单、调入通知单、直接调拨单的列表、新增编辑（本期提供新增/编辑的对象）、详情、弹窗与 Mock 回传链路；结果单不提供新增／编辑／取消／作废／重推入口，调入通知单不提供取消入口，调出与调入通知单不提供新增／编辑入口。
- Demo 入口超出 Demo PRD 登记：其他入库申请单、其他出库申请单在「取消中」状态各增加一个「模拟仓库回执」弹窗（仓库同意→已取消，拒绝→恢复原状态），用于演示取消中的回执分支；两份 Demo 弹窗 PRD 未登记该入口，待 04 确认是补进 Demo PRD 还是从页面移除。
- 结果单记账统一走 `lib/inventoryStockLogic.js` 的 `postStockEntries`（不允许负库存）；预占的占用与释放不记流水，冻结／解冻与结果单记账写流水。
- 其他出库申请单在途仓路径：审核通过后不推送仓库，由系统按申请数量直接生成已审核其他出库单并记账（申请单主PRD R20）；上游「按确认结果直接记账」的表述差异待上游同步。
- 演示数据限制：调拨已审核种子单的可回传范围依赖共享预占与在途库存，种子只覆盖部分行；完整链路建议现场新建单后审核、回传演示（机制与正式一致，仅演示造数受限）。
- 少收差异以「其他出库从在途逻辑仓扣减（业务类型盘亏）」手工建单办理，原型不提供从调拨行跳转预填。
