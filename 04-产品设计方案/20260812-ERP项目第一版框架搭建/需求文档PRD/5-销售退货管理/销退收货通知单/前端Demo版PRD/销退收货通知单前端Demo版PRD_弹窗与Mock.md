# 销退收货通知单前端Demo版PRD·弹窗与Mock

> **版本**：V0.2
> **更新时间**：2026-09-22
> **定位**：统一定义销退收货通知单各页触发的**确认弹窗**与 **Demo 专用 Mock** 行为。业务规则见[《销退收货通知单主PRD》](../销退收货通知单主PRD.md)（§6.4、§6.5、R06～R14；F05～F08）；**正式验收不以本章 Mock 为准。**
> **文档结构**：按[《前端Demo_弹窗与Mock模板-V2》](../../../../../../05-常用模板/02-模块设计交付/标准型页面&模块的PRD文档集/前端Demo_弹窗与Mock模板-V2.md)编写；本文为销退收货通知单落地样例。
> **通用口径**：弹窗可用条件服从主 PRD §6.4；下推创建字段校验以[新增编辑页 PRD §4](销退收货通知单前端Demo版PRD_新增编辑页.md) 为权威，本文不重复校验表。

关联资料：[《销售与履约需求分析》](../../../../../../02-需求调研和分析/04-销售与履约/销售与履约需求分析.md)（SAL-FR05、SAL-FR06、SAL-AC09、SAL-AC24）、[《销退收货通知单页面骨架》](../销退收货通知单页面骨架.md)。

列表、详情、新增/编辑页触发的弹窗在本文件维护；各页面 PRD 只写入口与确认后的页面表现，不重复弹窗正文。

---

## 1. 文档概述

| 项目 | 内容 |
| :--- | :--- |
| 本文职责 | 弹窗标题、正文、按钮、表单项；Mock 触发与本地数据规则 |
| 不在本文 | 页头按钮矩阵、字段含义、下推创建分层校验（见各页 PRD） |
| 引用方式 | 页面 PRD 写「见弹窗 PRD §x」 |

| 章节 | 功能编号 | 内容 |
| :--- | :--- | :--- |
| §2 | F05 | 取消通知单（待推送/推送失败） |
| §3 | F06 | 取消通知单（待收货） |
| §4 | F07 | 重试推送 |
| §5 | F08 | Demo Mock：模拟仓库回传 |
| §6 | — | 离开未保存（表单页） |
| §7 | — | 验收场景 |

下文 ASCII 图为**流程示意**，帮助快速建立顺序感；标题、正文、按钮文案以各节表格为准。

---

## 2. 取消通知单（F05）

**触发入口**：列表、详情「取消」；可用条件为**待推送**或**推送失败**（主 PRD §6.4）。

```
列表 / 详情
     │
点「取消」（待推送 / 推送失败）
     ▼
┌──────────────────────────────────────────┐
│  取消销退收货通知单                  [×] │
│  取消后通知不再执行，不可撤销…           │
│  推送失败时追加：请确认仓库尚未接收…     │
│  取消原因* [多行文本]                    │
│                    [取消] [确认取消]      │
└──────────────────────────────────────────┘
         │ 取消              │ 确认
         ▼                   ▼
    关闭弹窗，停留      已取消 + 额度回补或释放
                        Toast「已取消」
```

| 项目 | 内容 |
| :--- | :--- |
| 弹窗标题 | 取消销退收货通知单 |
| 正文 | 取消后通知不再执行，不可撤销，并回补退货单可下推量。 |
| 推送失败补充 | 推送失败时追加：「请确认仓库尚未接收本通知。」 |
| 必填项 | 取消原因（多行） |
| 确认按钮 | 确认取消 |
| 取消按钮 | 取消 |
| 确认后 | 单据状态→已取消；记录取消原因、取消时间、取消操作人；释放全部通知占用；**退货单仍开放→回补可下推量并保留原出库可退额度占用；退货单已关闭→释放相应可退额度、不允许继续安排**（主 PRD R09）；Toast「销退收货通知单已取消」 |

---

## 3. 取消通知单（待收货）（F06）

**触发入口**：列表、详情「取消」；可用条件为**待收货**（主 PRD §6.4）。

```
列表 / 详情
     │
点「取消」（待收货）
     ▼
┌──────────────────────────────────────────┐
│  取消销退收货通知单                  [×] │
│  单据已推送仓库，取消需等待仓库回执。    │
│  取消原因* [多行文本]                    │
│                    [取消] [确认取消]      │
└──────────────────────────────────────────┘
         │ 取消              │ 确认
         ▼                   ▼
    关闭弹窗，停留      取消中 + Toast
                        「已发起取消，等待仓库回执」
                              │
                    ┌─────────┴─────────┐
                    ▼                   ▼
              仓库同意              仓库拒绝
              已取消                待收货
```

| 项目 | 内容 |
| :--- | :--- |
| 弹窗标题 | 取消销退收货通知单 |
| 正文 | 单据已推送仓库，取消需等待仓库回执。取消期间通知仍占用退货单可下推量。 |
| 必填项 | 取消原因（多行） |
| 确认按钮 | 确认取消 |
| 取消按钮 | 取消 |
| 确认后 | 单据状态→取消中；Toast「已发起取消，等待仓库回执」 |

仓库回执处理（正式环境由集成层接收，Demo 见 §5）：

| 回执 | 结果状态 | 提示 |
| :--- | :--- | :--- |
| 仓库同意 | 已取消 | 销退收货通知单已取消；额度按 §2 确认后口径回补或释放 |
| 仓库拒绝 | 待收货 | 仓库拒绝取消，请继续等待收货 |

---

## 4. 重试推送（F07）

**触发入口**：列表、详情「重试推送」；可用条件为**推送失败**（主 PRD §6.4）；正式环境须已自动重试满3次仍失败（R14）。

```
列表 / 详情
     │
点「重试推送」（推送失败）
     ▼
┌──────────────────────────────────────────┐
│  重试推送仓库                          [×] │
│  将重新向仓库发送本通知，请确认失败原因已处理。│
│                    [取消] [确认重试]        │
└──────────────────────────────────────────┘
         │ 取消              │ 确认
         ▼                   ▼
    关闭弹窗，停留      推送中 + Toast
                        「已重新推送仓库」
```

| 项目 | 内容 |
| :--- | :--- |
| 弹窗标题 | 重试推送仓库 |
| 正文 | 将重新向仓库发送本通知，请确认失败原因已处理。 |
| 必填项 | 无 |
| 确认按钮 | 确认重试 |
| 取消按钮 | 取消 |
| 确认后 | 单据状态→推送中；Toast「已重新推送仓库」 |

---

## 5. Demo Mock：模拟仓库回传（F08）

**触发入口**：详情页「模拟仓库回传」；可用条件为**待收货**（Demo 环境）。

```
详情（待收货）
     │
点「模拟仓库回传」
     ▼
┌──────────────────────────────────────────┐
│  模拟仓库回传（Demo）                  [×] │
│  各行实收不能超过通知数量…               │
│  行号 | 商品编码 | 通知数量 | 实收数量   │
│  [ ] 零收（整单按取消结束）              │
│                    [取消] [确认回传]      │
└──────────────────────────────────────────┘
         │ 确认
         ▼
  有实收 → 已收货 + 生成销退入库单 Mock
  零收   → 已取消
```

### 5.1 弹窗规格

| 项目 | 内容 |
| :--- | :--- |
| 弹窗标题 | 模拟仓库回传（Demo） |
| 正文 | 各行实收不能超过通知数量；少于通知量按缺量结束。勾选零收则整单按取消结束。 |
| 表格列 | 行号、商品编码、商品名称、通知数量、实收数量（可编辑整数） |
| 零收勾选 | 勾选后各行实收锁定为 0，按零收取消处理 |
| 确认按钮 | 确认回传 |
| 取消按钮 | 取消 |

### 5.2 回传规则（本地 Mock）

| 项目 | 规则 |
| :--- | :--- |
| 实收上限 | 每行 0～通知数量；**允许少收、不允许超通知量** |
| 有实收 | 状态→已收货；回写实收、缺收；生成销退入库单 Mock；更新来源退货单累计实退与可下推量 |
| 零收 | 状态→已取消；释放全部通知占用；**不生成销退入库单**；额度按 §2 确认后口径回补或释放 |
| 标注 | 界面或 Toast 标注「演示数据」 |

### 5.3 不做的事

- 不代替正式仓库接口回传
- 不在 Mock 中自动推金蝶（销退入库单 Mock 可展示推送状态占位）
- 不把 Mock 行为写入主 PRD 正式验收

---

## 6. 离开未保存（表单页）

**触发入口**：下推创建页或编辑备注页点「取消」，且表单有未保存修改。

| 项目 | 内容 |
| :--- | :--- |
| 弹窗标题 | 确认离开？ |
| 正文 | 当前修改尚未保存，离开后将丢失。 |
| 确认按钮 | 确认离开 |
| 取消按钮 | 继续编辑 |
| 确认后 | 返回上一页，不保存 |

---

## 7. 验收场景

| 场景 | 操作 | 预期 |
| :--- | :--- | :--- |
| 待推送取消 | 填取消原因并确认 | 已取消；退货单开放→可下推量回补、可退额度占用保留 |
| 退货单已关闭后取消 | 退货单已关闭时确认取消 | 已取消；释放相应可退额度、不允许继续安排 |
| 推送失败取消 | 确认前看到仓库未接收提示 | 已取消 |
| 待收货取消 | 确认取消 | 取消中；Toast 等待回执 |
| 仓库同意取消 | Demo 模拟同意回执 | 已取消；额度按口径回补或释放 |
| 仓库拒绝取消 | Demo 模拟拒绝回执 | 恢复待收货 |
| 重试推送 | 推送失败单点重试 | 推送中 |
| Mock 少收 | 通知600，回传580 | 已收货；缺收20；退货单可下推回补缺量 |
| Mock 零收 | 勾选零收 | 已取消；不生成销退入库单 |
| 离开未保存 | 改备注后点取消 | 二次确认 |

Demo 验证弹窗文案、必填项与 Mock 本地数据联动；不代表生产接口、并发或后端事务行为。

---

## 修订记录

| 日期 | 变更摘要 |
| :--- | :--- |
| 2026-09-22 | V0.2：随TSV三项更新核对弹窗与Mock口径，无客户/商品展示限制类表述，结构无变化 |
| 2026-09-22 | V0.1 初版：取消、重试、Mock 回传、离开未保存 |
