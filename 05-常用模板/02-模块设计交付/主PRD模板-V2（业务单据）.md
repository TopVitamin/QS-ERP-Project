# [模块名或需求编号]-[需求名称]主PRD

> 模板版本：V2.1（业务单据）
> 适用于有状态流转、上下游单据衔接的业务单据类模块（采购订单、销售订单、通知单、结果单等）。主数据、纯配置、纯查询模块仍用《主PRD模板-V1》或《PRD模板-主数据-V1》，同一模块只写一份主PRD。
> 优先让人读懂业务边界、状态动作和可验收规则；页面布局、控件、完整文案和 Mock 行为在页面骨架与前端 Demo 版 PRD 展开。
> 示例和填写提示不是业务结论，定稿时替换为实际内容；不适用的小节整节删除，不留空架子。

## 1. 文档信息与阅读边界

| 项目 | 内容 |
| --- | --- |
| 版本 | [Vx.x] |
| 日期 | [YYYY-MM-DD] |
| 读者 | 业务负责人、产品、研发工程师、测试工程师 |
| 文档职责 | 描述本单据的业务目标、边界、功能、生命周期、业务规则与异常处理；不重复字段本体、完整跨模块流程、页面结构、系统权限、测试用例和技术实现 |
| 业务依据 | [需求分析文件及条款]、[业务设计文件及条款] |
| 字段依据 | [字段清单TSV（详细稿）]；字段名称、枚举、必填性、页面展示以 TSV 为准 |
| 状态依据 | [《单据与状态流转》对应章节，状态owner为外部文档]，或本 PRD 第六节（状态owner为本PRD） |
| 页面依据 | [页面骨架]；前端Demo版PRD已产出则关联目录，未产出写“待定（后续交付）”；按钮展示与文案以 Demo PRD 为准，**须服从本文状态—功能矩阵** |

| 版本 | 本次变化 | 状态 |
| --- | --- | --- |
| V0.1 | [变化摘要] | [草稿/待评审/已评审] |

## 2. 需求背景与目标

### 2.1 背景

[一至三段：当前问题、没有本单据时的痛点；已有上游分析时简述并引用，不重复整份调研。]

### 2.2 目标

- [可审核、可追溯的业务意图或执行依据]
- [数量/状态/下游如何追踪]
- [取消、关闭、分批执行等关键能力边界]

[可选短段：审核前后字段锁定原则；与上游 FR 编号呼应。]

## 3. 单据定位与业务边界

### 3.1 业务作用

| 项目 | 内容 |
| --- | --- |
| 单据层级 | [第1层业务安排 / 第2层执行指令 / 第3层结果单等，引用《单据分层设计》] |
| 核心职责 | [本单据记录什么、控制什么] |
| 单据来源 | [人工创建 / 上游下推 / 回传生成等] |
| 下游单据 | [名称与创建方式：手工 / 自动 / 回写触发] |
| 业务终结 | [取消 / 关闭 / 自动计算列等；写清是否可撤销] |

### 3.2 上下游关系摘要

> 只表达本单据与上下游的关键交接点；完整流程引用架构文档，不在此重复维护。

```mermaid
flowchart LR
    A["主数据或上游"] --> B["本单据"]
    B --> C["下游单据"]
```

### 3.3 业务对象关系

| 关系 | 说明 |
| --- | --- |
| [本单据 : 下游单] | [基数与含义] |

> 描述业务对象关系，不描述数据库表或接口模型。对象较多时可补概念关系图（erDiagram），标注为阅读辅助。

## 4. 核心业务场景

### 4.1 主路径概览

> 用三至六步说明读者最先需要理解的业务接力。完整跨模块流程仍引用业务设计。

| 步骤 | 参与方 | 动作或单据 | 关键结果 |
| --- | --- | --- | --- |
| 1 | [角色] | [动作/上游单据] | [进入本单据的条件或状态] |
| 2 | [角色] | [本单据动作] | [状态、数量或下游结果] |

### 4.2 场景索引

| 场景ID | 场景 | 类型 | 说明 |
| --- | --- | --- | --- |
| S01 | [场景名] | 主流程 | [路径摘要，含关键状态] |
| S02 | [场景名] | 支线 | [路径摘要] |

## 5. 范围与功能索引

### 5.1 范围

| 类别 | 说明 |
| --- | --- |
| 本期要做 | [本期功能与上下游衔接范围] |
| 本期暂不做 | [本模块后续可能承担、本期排除的能力] |
| 本单据不负责 | [长期职责边界；由哪个对象或模块负责] |

文档职责见第一节，不再列作本期未实现的功能。列表筛选、按钮位置和批量栏布局引用Demo PRD及本项目UI规范；是否提供某项业务能力仍须在本表或功能索引中明确。

### 5.2 功能与追溯索引

| 功能编号与名称 | 本次内容 | 上游依据 | 规则与状态依据 | Demo PRD | 验收编号 |
| --- | --- | --- | --- | --- | --- |
| F01 [功能名] | [摘要] | [文件及FR/BR/场景条款] | [R编号、状态章节] | [对应页面链接／待定（后续交付）] | [AC编号] |

新建模块写完整范围；调整类只展开变化及影响。功能编号贯穿状态矩阵、规则、Demo PRD与验收；页面尚未设计时保留“待定（后续交付）”，不补造页面规则。表格过宽时可拆出追溯表，但不重复填写功能说明。

## 6. 状态与操作

> 先在「状态依据」明确状态owner。外部文档为owner时，本节保留状态维度、本模块差异及状态—功能矩阵；状态取值说明、流转图和流转表仅在本模块与外部文档存在差异时补充。本文为owner时，写清状态定义与流转表；流转图、多维状态矩阵按复杂度保留，简单状态参考《状态机设计模板-V1》裁剪。状态下允许哪些业务动作在此定义；**用户是否拥有某动作权限不在本文件定义**。

### 6.1 状态维度

| 维度 | 取值 | 说明 |
| --- | --- | --- |
| [审核状态 / 单据状态 / 业务状态等] | [枚举] | [控制什么] |

[说明多字段并列时如何组合理解；计算列是否参与流转。]

### 6.2 状态取值说明

| 状态 | 含义 | 是否终态 |
| --- | --- | --- |
| [状态名] | [业务含义] | 是/否 |

### 6.3 状态流转图

[按维度分别画图，或画组合主路径；引用 mermaid stateDiagram。]

### 6.4 状态—功能矩阵

> ✅ = 允许；❌ = 不允许；✅（条件）= 允许但须满足前置条件。不受状态限制的查询功能可在表外统一说明，不固定占用F01编号。页面按钮须与本表一致，Demo PRD 引用本节编号。

| 功能编号 / 动作 | [状态组合1] | [状态组合2] | … |
| --- | --- | --- | --- |
| F02 编辑 | ✅ | ❌ | … |

### 6.5 状态流转表

| 当前状态 | 动作 | 前置条件 | 结果状态 | 二次确认 | 后置影响 | 失败处理 |
| --- | --- | --- | --- | --- | --- | --- |
| [组合状态] | [动作名] | [全部须满足] | [新状态] | [有/无] | [除改状态外的业务影响] | [阻断提示原则] |

## 7. 核心业务规则

> 按实际涉及的主题分组，不适用的小节删除；第五节只引用规则编号，不重复展开。

### 7.1 创建与提交规则

| 规则编号 | 主题 | 规则 | 边界或例外 |
| --- | --- | --- | --- |
| R01 | [主题] | [可判定规则] | [边界] |

涉及数量、金额时用一个标明「示例」的小算例。

### 7.2 审核与字段锁定规则

| 规则编号 | 主题 | 规则 | 边界或例外 |
| --- | --- | --- | --- |

### 7.3 执行与数量回写规则

[下游通知单/入库单/出库单的占用、回补、超收阻断等。]

### 7.4 价格与金额规则

[取价维度、锁定时机、零价处理等；引用价格管理 FR。]

### 7.5 取消、关闭与删除规则

[取消 vs 关闭分工、不可撤销、下游单据边界；删除若可选单独说明。]

### 7.6 业务生效点（按需）

| 业务动作 | 本单据影响 | 库存影响 | 业财/外部影响 |
| --- | --- | --- | --- |

### 7.7 字段校验绑定（按需）

> 完整字段定义见 TSV；本节只写「保存草稿 vs 提交审核」差异及跨字段规则。全局校验规范引用《6.强盛ERP的编码规则和通用字段规则》等。

| 字段 | 保存草稿 | 提交审核 | 模块覆盖说明 |
| --- | --- | --- | --- |

## 8. 上下游影响与协同（按需）

| 关联模块/系统 | 需要配合的变化 | 交接内容与时机 | 验收结果 |
| --- | --- | --- | --- |

## 9. 异常与一致性（按需）

### 9.1 并发操作

| 场景 | 业务处理结果 |
| --- | --- |

### 9.2 幂等与重复处理

| 操作 | 幂等规则 |
| --- | --- |

### 9.3 与下游单据一致性

| 场景 | 处理方式 |
| --- | --- |

> 只规定业务结果，不规定乐观锁等实现。

## 10. 验收标准与待确认事项

### 10.1 验收标准

| 编号 | 对应功能/规则 | 条件与操作 | 预期结果 |
| --- | --- | --- | --- |

覆盖关键成功路径及会影响业务结果的异常；不重复页面视觉验收。

### 10.2 待确认事项

| 编号 | 问题 | 影响范围 | 确认人 | 最迟确认阶段或时间 | 状态/结论 |
| --- | --- | --- | --- | --- | --- |

确认前不得写成最终规则；确认后同步正文、矩阵与 Demo PRD。


## 模板修订说明（成稿删除）

V2.1（2026-09-21）：合并范围声明、功能与追溯索引；关联入口集中在文首，按复杂度裁剪状态表达。保留业务生效点、异常一致性和业务验收。V1继续用于非单据类需求。
