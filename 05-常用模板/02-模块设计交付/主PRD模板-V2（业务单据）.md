# [模块名或需求编号]-[需求名称]主PRD

> 模板版本：V2（业务单据）
> 适用于有状态流转、上下游单据衔接的业务单据类模块（采购订单、销售订单、通知单、结果单等）。主数据、纯配置、纯查询模块仍用《主PRD模板-V1》或《PRD模板-主数据-V1》，同一模块只写一份主PRD。
> 优先让人读懂业务边界、状态动作和可验收规则；页面布局、控件、完整文案和 Mock 行为在页面骨架与前端 Demo 版 PRD 展开。
> 示例和填写提示不是业务结论，定稿时替换为实际内容；不适用的小节整节删除，不留空架子。

## 1. 文档信息与阅读边界

| 项目 | 内容 |
| --- | --- |
| 版本 | [Vx.x] |
| 日期 | [YYYY-MM-DD] |
| 读者 | 研发工程师、测试工程师、产品复核 |
| 文档职责 | 描述本单据的业务目标、边界、功能、生命周期、业务规则与异常处理；不重复字段本体、完整跨模块流程、页面结构、系统权限、测试用例和技术实现 |
| 业务依据 | [需求分析文件及条款]、[业务设计文件及条款] |
| 字段依据 | [字段清单TSV（详细稿）]；字段名称、枚举、必填性、页面展示以 TSV 为准 |
| 状态依据 | [《单据与状态流转》对应章节]或本 PRD 第六节（若本 PRD 为状态 owner） |
| 页面依据 | [页面骨架]、[前端 Demo 版 PRD 目录]；按钮展示与文案以 Demo PRD 为准，**须服从本文状态—功能矩阵** |

**不写什么**：[例如：不写下游 XX 单内部流程、不写金蝶报文、不写预付款流程等]

| 版本 | 本次变化 | 状态 |
| --- | --- | --- |
| V0.1 | [变化摘要] | [草稿/待评审/已评审] |

## 2. 需求背景与目标

### 2.1 背景

[一至三段：当前问题、没有本单据时的痛点；已有上游分析时简述并引用，不重复整份调研。]

### 2.2 目标

- [可审核、可追溯的业务意图或执行依据]
- [数量/状态/下游如何追踪]
- [取消、关闭、分批执行等关键能力边界]

[可选短段：审核前后字段锁定原则；与上游 FR 编号呼应。]

## 3. 需求范围

### 3.1 本期范围

- [列表、新增、编辑、详情及核心动作]
- [列表一期以行内操作为主；批量操作栏默认仅已选中计数，勾选配合页头导出；批量提交/审核等二期再定]
- [列表筛选：并列状态维度（审核/业务/收货或发货状态）默认多选，未选等同全部]
- [状态维护方式：单字段或多字段并列]
- [与下游单据的衔接方式]

### 3.2 功能清单

| 编号 | 功能/位置 | 本次内容 | 需求来源 |
| --- | --- | --- | --- |
| F01 | [功能名称或菜单位置] | [摘要] | [文件及条款] |

功能编号贯穿状态矩阵、规则引用、验收标准与 Demo PRD；新建模块写完整范围，调整类只展开变化部分。

### 3.3 需求追溯

| 上游场景与需求 | 本 PRD 功能 | 本 PRD 验收 |
| --- | --- | --- |
| [FR/BR 编号：摘要] | F0x | AC0x |

### 3.4 非本期范围

- [容易误解为本期的内容及归属]
- [系统权限、API、表结构、并发实现等]

## 4. 单据定位与业务边界

### 4.1 业务作用

| 项目 | 内容 |
| --- | --- |
| 单据层级 | [第1层业务安排 / 第2层执行指令 / 第3层结果单等，引用《单据分层设计》] |
| 核心职责 | [本单据记录什么、控制什么] |
| 单据来源 | [人工创建 / 上游下推 / 回传生成等] |
| 下游单据 | [名称与创建方式：手工 / 自动 / 回写触发] |
| 数量关系 | [1:N、分批、回补等] |
| 业务终结 | [取消 / 关闭 / 自动计算列等；写清是否可撤销] |

### 4.2 上下游关系摘要

> 只表达本单据与上下游的关键交接点；完整流程引用架构文档，不在此重复维护。

```mermaid
flowchart LR
    A["主数据或上游"] --> B["本单据"]
    B --> C["下游单据"]
```

### 4.3 业务对象关系

| 关系 | 说明 |
| --- | --- |
| [本单据 : 下游单] | [基数与含义] |

> 描述业务对象关系，不描述数据库表或接口模型。对象较多时可补概念关系图（erDiagram），标注为阅读辅助。

### 4.4 本单据不负责的事项

- [不直接改库存 / 不直接结算 / 不替代下游执行等]
- [已发生的下游事实不因本单取消而删除]

## 5. 核心业务场景索引

> 场景索引帮助读者和测试快速定位主路径与支线；完整角色接力见关联文档。

| 场景ID | 场景 | 类型 | 说明 |
| --- | --- | --- | --- |
| S01 | [场景名] | 主流程 | [路径摘要，含关键状态] |
| S02 | [场景名] | 支线 | [路径摘要] |

## 6. 状态与操作

> 已有权威状态设计时引用《单据与状态流转》对应节，并补充本模块差异。状态下允许哪些业务动作在此定义；**用户是否拥有某动作权限不在本文件定义**。

### 6.1 状态维度

| 维度 | 取值 | 说明 |
| --- | --- | --- |
| [审核状态 / 单据状态 / 业务状态等] | [枚举] | [控制什么] |

[说明多字段并列时如何组合理解；计算列是否参与流转。]

### 6.2 状态取值说明

| 状态 | 含义 | 是否终态 |
| --- | --- | --- |
| [状态名] | [业务含义] | 是/否 |

### 6.3 状态流转图

[按维度分别画图，或画组合主路径；引用 mermaid stateDiagram。]

### 6.4 状态—功能矩阵

> ✅ = 允许；❌ = 不允许；✅（条件）= 允许但须满足前置条件。查询类功能（F01）默认可用，不在此表重复。页面按钮须与本表一致，Demo PRD 引用本节编号。

| 功能编号 / 动作 | [状态组合1] | [状态组合2] | … |
| --- | --- | --- | --- |
| F02 编辑 | ✅ | ❌ | … |

### 6.5 状态流转表

| 当前状态 | 动作 | 前置条件 | 结果状态 | 二次确认 | 后置影响 | 失败处理 |
| --- | --- | --- | --- | --- | --- | --- |
| [组合状态] | [动作名] | [全部须满足] | [新状态] | [有/无] | [除改状态外的业务影响] | [阻断提示原则] |

## 7. 核心业务规则

> 按主题分组，便于研发和测试检索；功能节（第八节）只做索引，不重复展开。

### 7.1 创建与提交规则

| 规则编号 | 主题 | 规则 | 边界或例外 |
| --- | --- | --- | --- |
| R01 | [主题] | [可判定规则] | [边界] |

涉及数量、金额时用一个标明「示例」的小算例。

### 7.2 审核与字段锁定规则

| 规则编号 | 主题 | 规则 | 边界或例外 |
| --- | --- | --- | --- |

### 7.3 执行与数量回写规则

[下游通知单/入库单/出库单的占用、回补、超收阻断等。]

### 7.4 价格与金额规则

[取价维度、锁定时机、零价处理等；引用价格管理 FR。]

### 7.5 取消、关闭与删除规则

[取消 vs 关闭分工、不可撤销、下游单据边界；删除若可选单独说明。]

### 7.6 业务生效点（按需）

| 业务动作 | 本单据影响 | 库存影响 | 业财/外部影响 |
| --- | --- | --- | --- |

### 7.7 字段校验绑定（按需）

> 完整字段定义见 TSV；本节只写「保存草稿 vs 提交审核」差异及跨字段规则。全局校验规范引用《6.强盛ERP的编码规则和通用字段规则》等。

| 字段 | 保存草稿 | 提交审核 | 模块覆盖说明 |
| --- | --- | --- | --- |

## 8. 功能需求说明（页面索引）

> 页面区域、控件、校验时机、反馈文案在 Demo PRD；本节只保留功能与规则的对应关系。

| 编号 | 页面/位置 | 规则与状态依据 | Demo PRD |
| --- | --- | --- | --- |
| F01 | [列表] | R0x；§6.4 | [链接] |

## 9. 上下游影响与协同

| 关联模块/系统 | 需要配合的变化 | 交接内容与时机 | 验收结果 |
| --- | --- | --- | --- |

## 10. 关联文档与阅读入口

| 关联内容 | 阅读入口 | 本 PRD 与其边界 |
| --- | --- | --- |
| [业务流程/状态/字段/页面] | [完整文件名链接] | [谁 owner、本文只写什么] |

本文不重复定义：完整跨模块流程、详细字段表、页面布局与文案、权限配置、API 与表结构、独立测试用例脚本。

## 11. 异常与一致性（按需）

### 11.1 并发操作

| 场景 | 业务处理结果 |
| --- | --- |

### 11.2 幂等与重复处理

| 操作 | 幂等规则 |
| --- | --- |

### 11.3 与下游单据一致性

| 场景 | 处理方式 |
| --- | --- |

> 只规定业务结果，不规定乐观锁等实现。

## 12. 验收标准与待确认事项

### 12.1 验收标准

| 编号 | 对应功能/规则 | 条件与操作 | 预期结果 |
| --- | --- | --- | --- |

覆盖关键成功路径及会影响业务结果的异常；不重复页面视觉验收。

### 12.2 待确认事项

| 编号 | 问题 | 影响范围 | 状态/结论 |
| --- | --- | --- | --- |

确认前不得写成最终规则；确认后同步正文、矩阵与 Demo PRD。

---

**与 V1 模板的差异**：V2 强化单据定位、场景索引、状态全套餐（矩阵 + 流转表）、规则分组、业务生效点、关联阅读入口与异常一致性；功能细节下沉到 Demo PRD，主 PRD 以可判定业务契约为中心。V1 仍可用于非单据类需求。
