# [模块名或需求编号]-[需求名称]主PRD

> 模板版本：V1
> 适用于ERP业务单据、配置规则、查询展示及已有功能调整。**有状态流转、上下游单据衔接的业务模块优先用《主PRD模板-V2（业务单据）》。** 主数据可选用《PRD模板-主数据-V1》，同一模块只写一份主PRD。
> 优先让人读懂需求、判断方案和完成评审，AI辅助整理。主PRD与前端Demo版PRD分别交付，简单模块也不合并。
> 简单功能用短表说明，复杂功能单独展开；可选章节不适用时整节删除。示例和填写提示不作为业务结论，定稿时替换为实际内容。

## 1. 文档概要

| 版本 | 本次变化 | 状态 | 更新日期 | 负责人 |
| --- | --- | --- | --- | --- |
| V1.0 | [变化摘要] | [草稿/待评审/已评审/已上线] | [日期] | [姓名] |

需求类型：[新建模块/功能调整/业务扩展]。

关联资料：[需求分析文件及条款]、[业务设计文件及条款]、[字段清单TSV]、[页面骨架]、[前端Demo版PRD]。填写真实链接；用户已确认的设计注明确认结论。

关键评审结论按需记录；结论确认后同步正文，避免只能从会议记录找到最终方案。

## 2. 需求背景与范围

用一至三段说明当前问题、需求触发原因和预期结果。已有上游分析时简述并引用，不重复整份调研。

| 范围 | 说明 |
| --- | --- |
| 本期要做 | [本次解决的问题和功能边界] |
| 本期不做 | [容易误解为本期的内容及归属] |
| 适用对象 | [业务类型、组织、仓库、渠道等实际适用条件；无关维度不写] |

## 3. 方案总览与功能清单

先用短段说明整体方案。涉及多步骤或跨系统协作时补流程图或时序图；已有权威流程、对象关系或状态文档时引用对应章节，不重复维护。

| 功能编号 | 功能/位置 | 本次内容 | 关联模块或系统 | 需求来源 |
| --- | --- | --- | --- | --- |
| F01 | [功能名称或菜单位置] | [新建内容或改动摘要] | [关联对象；无则填无] | [文件及条款/确认结论] |

功能编号用于关联详细说明和验收。新建模块写完整功能范围；调整类需求只展开本次变化及受影响部分。

## 4. 功能需求说明

### 4.1 共用业务规则（按需）

多个功能共用的计算、取值、转换或匹配规则在此定义，其他位置引用编号。单个功能独有的规则放在该功能下。

| 规则编号 | 适用条件 | 规则与结果 | 边界或例外 |
| --- | --- | --- | --- |
| R01 | [何时适用] | [可判定的规则或公式] | [边界处理；无则填无] |

涉及数量、金额、换算或分配时，用一个标明“示例”的小算例说明输入、计算和结果；精度、舍入及尾差有影响时写清。

### 4.2 [F01 功能名称]

简单功能可直接使用下表；内容较多时改用短段落和小节，不把长篇规则挤进单元格。

| 内容 | 说明 |
| --- | --- |
| 使用场景 | [谁在什么情况下需要完成什么事] |
| 现状与调整后 | [调整类必填；新建功能改写为功能说明] |
| 操作与处理 | [人工操作或系统事件、前置条件、校验、成功结果] |
| 业务规则 | [本功能规则，或共用规则编号] |
| 异常与恢复 | [失败原因、是否保留已有结果、如何修正或继续] |
| 关联影响 | [其他记录、上下游单据、数量或状态受到的影响] |

按实际功能复制本节。字段定义引用TSV；页面布局、控件、完整提示文案和校验时机在Demo PRD展开。业务上必须明确的失败原因和处理结果仍在此写清。

### 4.3 状态与动作（按需）

有状态流转时使用[《状态机设计模板-V1》](状态机设计模板-V1.md)。只涉及一个功能的状态可放在该功能下；跨功能共用的状态集中写在此处。已有权威状态设计时引用，并补充本次变化。

### 4.4 按业务类型补充（填写后删除本提示节）

只展开本次实际涉及的内容，不为凑齐下表增加需求。

| 业务类型 | 需要说明的重点 |
| --- | --- |
| 业务单据 | 来源与上下游关系、头行结构、数量金额、分批执行、取消关闭及逆向处理 |
| 主数据 | 编码防重、引用关系、启停、修改对历史业务的影响；审批和同步按需 |
| 配置规则 | 适用范围、匹配优先级、冲突、有效期、未命中处理、历史结果是否变化 |
| 查询展示 | 数据来源、统计定义、筛选与汇总关系、更新时间、空数据与异常情况 |
| 已有功能调整 | 调整前后差异、适用新旧数据、存量单据和原有流程是否受影响 |

重复操作、并发、部分成功和外部失败，仅在确实影响业务结果时说明预期行为，不指定数据库或锁实现。

## 5. 上下游影响与协同（按需）

| 关联模块/系统 | 需要配合的变化 | 交接内容与时机 | 负责方 | 验收结果 |
| --- | --- | --- | --- | --- |
| [名称] | [改动或配置] | [输入输出、触发时机、失败处理] | [团队/联系人] | [可验证结果] |

涉及字段转换时写清业务映射与单位换算，完整字段属性引用TSV，报文细节引用接口文档。历史数据兼容与迁移有影响时写清处理范围；已在功能章节说明的直接引用。

## 6. 验收标准与待确认事项

### 6.1 验收标准

| 编号 | 对应功能/规则 | 条件与操作 | 预期结果 |
| --- | --- | --- | --- |
| AC01 | F01/R01 | [输入及操作] | [可直接判断的结果] |

覆盖关键成功路径及会影响业务结果的异常，不重复页面视觉验收。

### 6.2 待确认事项（按需）

| 编号 | 问题 | 影响范围 | 确认人 | 最迟确认时间 | 状态/结论 |
| --- | --- | --- | --- | --- | --- |
| Q01 | [未定事项] | [影响哪些功能或验收] | [姓名/角色] | [时间] | [待确认/已确认结论] |

确认后同步相关正文和验收；尚未确认的建议不能写成最终规则。

## 7. 上线准备（可选）

仅在本次涉及配置、初始化、迁移或外部依赖时保留。

| 阶段 | 准备事项 | 负责方 | 前置依赖 | 完成标准 |
| --- | --- | --- | --- | --- |
| [上线前/后] | [实际任务] | [负责方] | [依赖] | [检查结果] |
