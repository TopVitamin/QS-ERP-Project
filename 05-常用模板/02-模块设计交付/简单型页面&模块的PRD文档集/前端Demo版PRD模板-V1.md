# [模块名]前端Demo版PRD

> 模板版本：V1
> 适用于页面和交互较少的模块。仍单独交付主PRD，本文件集中描述实际页面，不承担主PRD职责。
> 版本：[版本/日期]。

## 配套依据与填写方式

关联资料：[主PRD及规则编号]、[字段清单TSV]、[页面骨架]、[《09-产品原型/AGENTS.md》](../../../../09-产品原型/AGENTS.md)、[UI布局公共说明](../UI布局公共说明.md)。

业务条件引用主PRD，字段属性引用TSV。布局须与页面骨架及 UI布局公共说明一致；样式与交互以 AGENTS 为准。

删除不适用的章节和示例行；实际文案直接填写，不只写“提示成功”或“报错”。未知内容写“待确认”。

## UI布局（与骨架、09原型一致）

列表、表单、详情区域的6列栅格、标签上下结构、备注占3列、批量操作栏等规则见[UI布局公共说明](../UI布局公共说明.md)；各区域字段位置以模块页面骨架为准。

## 列表与查询

| 内容 | 设计 |
| --- | --- |
| 页面名称与入口 | [标题、菜单/来源页] |
| 页面区域 | [按已确认骨架列出区域顺序] |
| 展示数据范围 | [业务范围，引用主PRD] |
| 默认排序与分页 | [字段、方向、每页条数] |

### 查询条件

| 顺序 | 字段 | 控件 | 查询方式 | 默认值/占位文案 | 联动与触发 |
| --- | --- | --- | --- | --- | --- |
| 1 | [TSV字段名] | [输入框/下拉/日期范围等] | [精确/模糊/范围等] | [实际文字] | [搜索触发及关联变化] |

说明搜索、重置和条件变化后是否回到首页，是否保留条件；采用公共规则时引用具体规范。

**状态类筛选（业务单据）**：审核状态、业务状态、收货/发货状态等并列维度默认**多选**；未选等同全部，选中多项 OR 匹配。

### 列表展示

| 顺序 | 列名/字段 | 显示形式与示例 | 空值显示 | 排序/固定 | 点击行为 |
| --- | --- | --- | --- | --- | --- |
| 1 | [字段] | [文字/标签/链接及格式] | `-`（未填写时） | [按需] | [进入页面/无] |

精度与枚举引用TSV。列设置、汇总、导出和勾选按实际需求说明，不默认增加。勾选列无批量业务操作时，在「说明」列写明用途（如配合导出已勾选）。

### 按钮与行操作

| 位置 | 按钮文案 | 可用条件及来源 | 点击行为 | 确认/校验 | 成功与失败后的表现 |
| --- | --- | --- | --- | --- | --- |
| [页头/工具栏/行内] | [实际文案] | [主PRD规则] | [页面、弹窗或动作] | [章节引用/无] | [刷新、保留、跳转及反馈引用] |

不可用动作需明确隐藏、禁用或说明原因。**批量操作栏**与**行内操作**分开写：主数据批量提交审核等写「批量操作栏」；业务单据一期若无批量业务按钮，写「仅已选中计数，业务动作在行内」。批量操作如存在，写清选择范围、同质状态要求、跨页选择和部分失败结果。

## 详情页或查看容器

| 内容 | 设计 |
| --- | --- |
| 标题与入口 | [实际标题规则、来源页] |
| 容器形式 | [独立页/弹窗/抽屉] |
| 区域顺序 | [业务分组、明细、关联信息等，按实际骨架] |
| 返回/关闭 | [返回目标、是否保留原查询和页码] |

| 分组 | 字段/明细列 | 显示形式 | 空值及长文本 | 关联跳转 |
| --- | --- | --- | --- | --- |
| [分组] | [TSV字段] | [文字、标签、表格等] | [表现] | [去向/无] |

有子表、关联单据、日志或执行结果时补充展示方式；不因模板存在就新增业务内容。

| 按钮文案 | 位置 | 可用条件及来源 | 点击行为 | 确认与反馈 |
| --- | --- | --- | --- | --- |
| [动作] | [位置] | [主PRD规则] | [行为] | [实际文案或共用章节引用] |

## 新增/编辑页或表单容器（按需）

| 模式 | 入口与标题 | 容器形式 | 默认值来源 | 保存成功后 |
| --- | --- | --- | --- | --- |
| 新增 | [入口/标题] | [页面/弹窗/抽屉] | [TSV或规则] | [跳转或停留] |
| 编辑 | [入口/标题] | [形式] | [原记录] | [跳转或停留] |

### 表单内容

| 分组/顺序 | 字段 | 控件与占位文案 | 新增/编辑表现 | 联动 | 约束来源 |
| --- | --- | --- | --- | --- | --- |
| [分组/顺序] | [TSV字段] | [控件和实际文字] | [编辑/只读/不展示] | [变化后的带出、清空或重算] | [TSV字段/主PRD编号] |

系统字段、状态字段按实际规则展示和操作，不默认增加编辑能力。子表如需维护，说明添加、删除、排序、默认项及行级错误定位。

### 操作与校验

| 动作 | 业务条件来源 | 校验时机 | 校验失败的定位和文案 | 成功后的处理 |
| --- | --- | --- | --- | --- |
| [保存] | [规则/字段] | [输入/失焦/保存] | [字段、明细行或摘要；实际文字] | [结果] |
| [取消/返回/关闭] | [条件] | [是否有未保存修改] | [离开确认文案或无需确认] | [返回目标及数据保留] |

保存过程中是否防止重复点击、失败是否保留输入、重新进入如何加载应写清。仅在业务确有“保存并提交”时说明分步结果与提交失败后的页面状态。

## 页面状态与反馈文案

| 场景 | 触发条件 | 页面表现/内容保留 | 显示文案 | 后续操作 |
| --- | --- | --- | --- | --- |
| [加载/无数据/无查询结果/加载失败] | [条件] | [表现] | [实际文字] | [重试、重置等] |
| [动作成功/失败] | [动作及结果] | [刷新、跳转或保留内容] | [实际文字] | [继续操作方式] |

### 确认弹窗（按需）

| 操作 | 弹窗标题 | 正文 | 确认按钮 | 取消按钮 | 确认后的处理 |
| --- | --- | --- | --- | --- | --- |
| [需确认的动作] | [文案] | [后果说明] | [文案] | [文案] | [成功/失败行为或章节引用] |

只为实际需要确认的动作填写，不自动给所有操作加弹窗。

## 演示数据与验收

演示数据使用虚构内容，并标明为示例；状态和枚举来自TSV。按实际能力覆盖正常、空值、长文本、失败及边界，不为凑数量增加无关状态或字段。

| 场景 | 示例条件 | 操作 | 预期页面结果 |
| --- | --- | --- | --- |
| [关键正常路径] | [虚构数据] | [动作] | [显示、反馈和结果] |
| [实际存在的异常或边界] | [数据] | [动作] | [阻断、提示和恢复] |

模拟的保存、状态变化和关联结果应可连续演示；Demo验证不代表真实接口、并发或后端事务已验证。
