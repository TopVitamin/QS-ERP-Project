# [模块名]前端Demo版PRD·列表页

> **模板版本**：V2
> **更新时间**：[YYYY-MM-DD]
> **定位**：供 AI 生成列表页前端原型使用。字段定义见[《[对象名]（详细稿）》]([TSV路径])；业务规则见[《[模块名]主PRD》]([主PRD路径])（[规则章节引用]）。
> **本文件只描述页面呈现规则，不重复定义字段含义与枚举取值（枚举一律以字段清单为准）。**

关联资料：[《[模块名]主PRD》]([主PRD路径])（[功能编号]）、[《[模块名]页面骨架》]([骨架路径])、[《[模块名]前端Demo版PRD_弹窗与Mock》]([弹窗PRD路径，无则删])、[《原型开发说明》](../../../09-产品原型/原型开发说明.md)（查询区通用规则、FilterControl type、列表列通用呈现规则）。

共享弹窗正文和共享Mock行为统一在《[模块名]前端Demo版PRD_弹窗与Mock》维护；本页只写入口、确认后的页面表现、本页数据覆盖与验收。没有独立弹窗文档时，指定一份页面PRD维护共享内容，其他页面引用，避免各写一套。

删除不适用的章节和示例行；实际文案直接填写，不只写「提示成功」或「报错」。未知内容写「待确认」。

---

## 1. 页面目标与范围

- 页面名称：[列表页名称]
- 查看视角：[仅单据视角 / 单据视角+单据行视角；未做则写明]
- 作为 [新增、导入、详情、导出等] 的统一入口
- [一期以行内操作为主 / 支持批量操作等]
- 功能与状态—操作矩阵见[《[模块名]主PRD》]([主PRD路径]) [章节]；本页落地 [功能编号] 及列表侧行操作入口

> **本期边界**（按需填写，写清本期不做或仅 Mock 的能力）：
> - 页头导出：[正式能力 / 不做]；导入：[正式 / 仅 Demo Mock]
> - 批量操作栏：[无业务按钮说明 / 支持的批量动作]
> - 联查、单据行视角、红冲等：[本期不做则写明]
> - [模块特有的取消/关闭/下游联动口径，引用主 PRD 规则编号]

---

## 2. 查询区

> 可筛选字段与顺序以[《[对象名]（详细稿）》]([TSV路径])「可筛选=是」为准；下表为默认查询方案呈现顺序。
> **type 列**：与原型 `FilterControl` 的 `type` 一致，完整规范见[《原型开发说明》](../../../09-产品原型/原型开发说明.md)「查询区通用规则」「FilterControl 控件 type」。布局、折叠、重置/查询行为不在本文重复定义。
> **实现参照**（可选）：`09-产品原型/src/pages/[XxxListPage].jsx` 的 `filterFields`、`initialFilters`。

| 查询字段 | type | 匹配 | placeholder / 说明 |
| :--- | :--- | :--- | :--- |
| [字段名] | `[search/select/searchable-select/multi-select/date-range/date]` | [精确/模糊/范围/OR] | [实际 placeholder 与说明] |

**状态类筛选（业务单据）**：审核状态、业务状态、收货/发货状态等并列维度用 `multi-select`；初值 `[]`、未选=全部；选中多项 OR 匹配。

查询区交互（收起过滤、重置、查询、空条件默认、查询后回第一页）见《原型开发说明》「查询区通用规则」。默认排序统一写在第4.3节。

---

## 3. 工具栏按钮

| 位置 | 按钮 | 显示条件 | 说明 |
| :--- | :--- | :--- | :--- |
| 页头 | [新增] | [始终 / 条件] | [行为] |
| 页头 | [导入(Mock)] | [按需] | [仅 Demo 则引用 §6] |
| 页头 | 导出 | [始终 / 有筛选结果时] | [异步任务 / 同步下载；范围选项] |
| 列表工具条 | 列设置 | 工程具备时 | 列显隐/排序/固定 |
| 批量操作区 | [批量动作或「无业务按钮」] | [条件] | [说明] |

页头主操作可有图标；批量栏、行内操作不加图标（见《原型开发说明》「列表列通用呈现规则」）。

---

## 4. 列表表格

### 4.1 视角说明

[每行一张单 / 单据行视角规则；商品级筛选项是否展开为多行]

### 4.2 列定义

通用对齐、千分位、空值、列表状态样式见原型规范「列表列通用呈现规则」。下表保留完整列清单；采用公共规则的单元格填“沿用公共规则”，整列均无差异时可删除该属性列，不重复抄写默认值。固定位置、点击行为等列级差异在此维护。

| # | 列名 | 展示形式 | 对齐 | 排序 | 空值 | 格式/示例 | 说明 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 勾选 | 复选框 | 居中 | — | — | — | [用途，如配合导出「已勾选」] |
| 2 | [单号] | 链接 | 左 | 否 | — | `[示例]` | 固定左侧；点击进详情；行内不设「查看」 |
| … | [字段] | [文本/日期/数值/金额/语义色文字/链接] | [左/右] | [是/否] | [`-` / `0` / `0.00`] | `[示例]` | [Code-Name、ellipsis 等] |
| n | 操作 | 行内按钮 | 左 | — | — | [示例按钮] | 固定右侧；见 §6；最多 3 个直出 +「更多」 |

列顺序原则：[本模块排序原则]。不展示 [无依据列]。

**实现参照**（可选）：`09-产品原型/src/data/[xxxData].js` 的 `tableColumns`。

### 4.3 本页列表约定

| 项目 | 规则 |
| --- | --- |
| 默认排序 | `[字段]` [升序/降序] |
| 币别与金额 | [有币别列时：`{编码} {名称}` 独立列，金额列不含币别符号；无则删本行] |
| 状态tone映射 | [列表用语义色文字，不用 StatusBadge；写明各枚举→tone 映射] |
| [其他模块差异] | [行操作简称、rowSpan 规则等] |

关联选择类主数据（供应商、客户、仓库等）在列表列、筛选下拉中默认 Code-Name（`{编码} {名称}`），见《6.强盛ERP的编码规则和通用字段规则》2.3。

---

## 5. 分页与勾选规则

### 5.1 分页

采用原型规范的分页规则；仅在存在模块差异时补充。

### 5.2 勾选

- 勾选键以 **[单据 ID / 行 ID]** 为单位。
- [勾选用途：导出已勾选 / 批量操作等]

---

## 6. 行操作按钮

行内操作按使用频率从左到右排列；**仅展示当前状态允许的操作**（不满足时隐藏，不用 disabled）。风险动作须二次确认；弹窗正文与校验见[《[模块名]前端Demo版PRD_弹窗与Mock》]([弹窗PRD路径])（独立弹窗 PRD 时引用；否则引用指定页面PRD中的共享确认说明）。

### 6.1 操作矩阵

| 按钮 | [状态列1] | [状态列2] | … | 说明 |
| :--- | :---: | :---: | :---: | :--- |
| [编辑] | ✅ | ❌ | … | [行为摘要] |

单号列可点击进入详情，行内不设「查看」。页头按钮见 §3。

---

## 7. 导入（按需）

[一期正式不做则写「仅 Demo Mock」；步骤结构：下载模板 → 上传 → 校验预览 → 结束/提交任务]

---

## 8. 反馈规范

| 操作 | 反馈方式 | 文案 |
| :--- | :--- | :--- |
| 查询成功 | 无额外反馈 | 直接刷新列表结果 |
| 查询失败 | Toast | [实际文案] |
| [动作成功] | Toast | [实际文案] |
| [动作失败——原因] | Toast | [实际文案] |
| 导出 | Toast | [异步：导出任务已创建… / 同步：导出成功] |
| 操作失败——状态不匹配 | Toast | 当前单据状态为{状态}，不可执行{动作} |
| 操作失败——权限不足 | Toast | 您暂无{动作}权限，请联系管理员 |

列表页触发的确认弹窗，统一见弹窗 PRD 或本节上方引用；不重复维护两套文案。

---

## 9. Mock 数据要求与演示验收

### 9.1 Mock 数据覆盖

- 列表至少 **[N] 条** [单据/记录]
- [各状态、边界数据覆盖要求；枚举必须落在 TSV 范围内]

### 9.2 演示验收

| 场景 | 前置 | 操作 | 预期 |
| --- | --- | --- | --- |
| [正常列表] | [Mock 条件] | [操作] | [预期，含列格式、默认排序] |
| [筛选/行操作/阻断] | [条件] | [操作] | [预期] |

模拟的保存、状态变化和关联结果应可连续演示；Demo 验证不代表真实接口、并发或后端事务已验证。

---

## 修订记录

| 日期 | 变更摘要 |
| :--- | :--- |
| [日期] | [本模块首次设计或变更摘要，不复制模板修订历史] |
