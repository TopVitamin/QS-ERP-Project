# [模块名]前端Demo版PRD·弹窗与Mock

> **模板版本**：V2
> **更新时间**：[YYYY-MM-DD]
> **定位**：统一定义[模块名]各页触发的**确认弹窗**与 **Demo 专用 Mock** 行为。业务规则见[《[模块名]主PRD》]([主PRD路径])（[§6.4、规则编号、功能编号引用]）；**正式验收不以本章 Mock 为准。**
> **通用口径**：弹窗可用条件服从主 PRD §6.4；提交前字段校验以[新增编辑页 PRD §4]([新增编辑PRD路径，无提交流则删]) 为权威，本文不重复校验表。
> **样板参考**：[《采购订单前端Demo版PRD_弹窗与Mock》](../../../04-产品设计方案/20260812-ERP项目第一版框架搭建/需求文档PRD/2-采购管理/采购订单/前端Demo版PRD/采购订单前端Demo版PRD_弹窗与Mock.md)（V0.5.1）。

关联资料：[《[模块名]前端Demo版PRD_列表页》]([列表PRD路径])、[《[模块名]前端Demo版PRD_详情页》]([详情PRD路径])、[《[模块名]前端Demo版PRD_新增编辑页》]([新增编辑PRD路径，无则删])、[《原型开发说明》](../../../09-产品原型/原型开发说明.md)（列表页 `*ActionDialogs` 接线）。

列表、详情、新增/编辑页触发的弹窗在本文件维护；各页面 PRD 只写入口与确认后的页面表现，不重复弹窗正文。

删除不适用的章节和示例行；实际文案直接填写。未知内容写「待确认」。

---

## 1. 文档概述

| 项目 | 内容 |
| :--- | :--- |
| 本文职责 | 弹窗标题、正文、按钮、表单项；Mock 触发与本地数据规则 |
| 不在本文 | 页头/行内按钮矩阵、字段含义、保存/提交分层校验（见各页 PRD） |
| 引用方式 | 页面 PRD 写「见弹窗 PRD §x.x」 |

| 章节 | 功能编号 | 内容 |
| :--- | :--- | :--- |
| §2 | [Fxx] | [提交相关弹窗，无则删整节] |
| §3 | [Fxx] | [状态变更类确认弹窗] |
| §4 | [Fxx] | [Demo Mock：主链串联，无则删] |
| §5 | — | [其他 Demo Mock，如导入向导，无则删] |
| §6 | — | 验收场景 |

**流程图按项目要求使用**：强盛ERP的弹窗与Mock说明中，流程类章节配ASCII示意；普通确认框填写表格即可，无需重复画相同线框。跨项目使用时按目标项目要求裁剪。

- **建议画**：多步顺序（提交链）、分支（Mock 开关）、入口阻断（按钮不可见 vs Toast vs 弹窗）。
- **不必画**：每个 §3.x 结构相同的确认框。
- **权威细则**：标题、正文、按钮文案以各节表格为准；ASCII 仅为流程示意。

图表须表达相同流程；具体条件与文案在表格中维护，发现冲突时同步修正图示。

---

## 2. 提交相关弹窗（[功能编号，无则删整节]）

**触发入口**：[新增/编辑页、列表、详情「提交」等]；可用条件为 **[状态条件，引用主 PRD §6.4]**。

**校验前置**：打开任何提交弹窗前，先按主 PRD [规则编号] 与[新增编辑页 §4.4]([路径])校验。未通过则 Toast 或字段提示阻断，**不进入**下列弹窗。列表/详情提交已有草稿时，校验口径与表单页一致。

**弹窗顺序**：[如：先零价确认（如有）→ 再提交确认；或单步确认]。

```text
[列表／详情／新增编辑页点“提交”]
    -> [新增编辑页第4.4节校验]
         | 失败 -> [字段提示或Toast，停留当前页]
         | 通过 -> [是否需要中间确认]
                      | 不需要 ----------------------+
                      | 需要 -> [中间确认]           |
                                  | 取消 -> [停留]   |
                                  | 继续 ------------+
                                                     v
                                               [提交确认]
                                                 | 取消 -> [停留]
                                                 | 确认 -> [执行提交]
                                                             | 失败 -> [提示与恢复]
                                                             | 成功 -> [目标状态与反馈]
```

### 2.1 [中间确认弹窗名，无则删]

| 项目 | 内容 |
| :--- | :--- |
| 出现条件 | [如任一明细行价格为 0] |
| 弹窗标题 | [标题] |
| 正文 | [正文；含行号占位 {N}] |
| 确认按钮 | [文案] |
| 取消按钮 | 取消 |
| 确认后 | 进入 §2.2 提交确认弹窗 |

### 2.2 提交确认

| 项目 | 内容 |
| :--- | :--- |
| 弹窗标题 | [标题] |
| 正文 | [正文] |
| 确认按钮 | [文案] |
| 取消按钮 | 取消 |
| 确认后 | [引用主PRD的状态结果与共享反馈；页面刷新或跳转见来源页PRD] |

---

## 3. 状态变更弹窗（[功能编号]）

下列弹窗均在**二次确认**后执行。入口可用条件见主 PRD §6.4 与各页页头/行内矩阵；条件不满足时**不展示按钮**，无需弹窗阻断。

**阻断类失败**（不满足业务规则、未进入成功流）：[写明哪些操作先 Toast，引用规则编号与示例文案]。

普通确认框按第3.1节填写，无需另画相同线框。复杂表单或明细弹窗可补图。入口不满足状态条件时隐藏；入口可见但业务校验失败时，按引用规则提示并阻断，不进入成功流程。

### 3.1 [操作名，如审核]

| 项目 | 内容 |
| :--- | :--- |
| 触发入口 | [列表、详情「审核」等] |
| 可用条件 | [状态条件] |
| 弹窗标题 | [标题] |
| 正文 | [正文] |
| 必填项 | [无 / 字段说明] |
| 确认按钮 | [文案] |
| 取消按钮 | 取消 |
| 确认后 | [状态与字段回写]；Toast「[文案]」；[是否接续 §4 Mock] |

### 3.2 [操作名]

（结构同 §3.1；按模块实际弹窗增删小节，勿合并为宽表。）

### 3.x 离开未保存（表单页，无则删）

| 项目 | 内容 |
| :--- | :--- |
| 触发入口 | 新增/编辑页点「取消」或路由离开，且存在未保存修改 |
| 弹窗标题 | 离开当前页面？ |
| 正文 | 离开后未保存的内容将丢失。 |
| 确认按钮 | 确认离开 |
| 取消按钮 | 取消 |
| 确认后 | 返回列表或上一页 |

表单页错误展示与文案口径见[新增编辑页 §4.6、§7]([路径])。

---

## 4. Demo Mock：[场景名]（[功能编号，无则删整节]）

### 4.1 正式规则

[用业务语言写正式环境应该怎样；引用需求分析或主 PRD 编号。]

本节 Mock **仅用于 Demo 原型串联**，不属于 [验收编号] 的正式验收项。

### 4.2 触发时机

| 时机 | 行为 |
| :--- | :--- |
| [如审核成功后] | [弹出引导弹窗／出现Mock按钮；采用开关时写明默认值] |

采用Demo开关时可使用以下简图，按实际触发和分支替换；未采用开关时删除开关判断。

```text
[前置动作成功] -> [Demo开关]
                    | 关闭 -> [结束]
                    | 开启 -> [引导弹窗]
                                | 关闭/次按钮 -> [对应结束或返回行为]
                                | 主按钮 -> [第4.4节本地数据规则]
```

### 4.3 引导弹窗（或 Mock 操作弹窗）

| 项目 | 内容 |
| :--- | :--- |
| 标题 | [标题] |
| 正文 | [正文；区分正式流程与 Demo] |
| 主按钮 | [文案] |
| 次按钮 | [文案] |
| 关闭图标 | [关闭后行为] |
| 表单项（可选） | [如 Mock 回传带明细表，列清单与详情一致] |

### 4.4 生成/回传规则（本地 Mock）

| 项目 | 规则 |
| :--- | :--- |
| 来源 | [当前单据 / 上游单据] |
| 目标单据 | [下游单据类型与初始状态] |
| 明细 | [行映射与数量规则] |
| 单号 | [编码规则 / fictitious] |
| 占用与回写 | [订单可下推量、在途量等] |
| 外部系统 | Demo 不调用 [WMS/金蝶等]；可配置延时模拟 |

界面标注「演示数据」或「Mock」角标，与正式手工创建区分。

### 4.5 不做的事

- [不代替的正式页面能力]
- [不在 Mock 中自动执行的外部动作]
- [不把 Mock 写入主 PRD 验收标准]

### 4.6 与正式流程的关系

[关闭引导后如何走正式下推/创建；下游 Mock 交叉引用其他模块弹窗 PRD §x。]

---

## 5. Demo Mock：[其他场景，无则删整节]

[如列表导入向导、仅演示不上线的批量操作。步骤细节若在列表页 PRD 已写，本文只补弹窗/向导内文案。]

有多步向导时按实际步骤替换下图，补明返回、重试和结束路径；不涉及本场景则删除整节。

```text
[入口] -> [步骤一] -> [步骤二] -> [结束]
                       | 失败 -> [提示与修正] -> [重试步骤二]
```

| 场景 | 文案或表现 |
| :--- | :--- |
| 向导内提示 | [文案] |
| 校验预览 | [Mock 表现] |
| 结束 | [明确不写正式数据] |

---

## 6. 验收场景

| 场景 | 操作 | 预期 |
| :--- | :--- | :--- |
| [提交流] | [步骤] | [预期] |
| [提交阻断] | [步骤] | Toast 阻断，不进入目标状态 |
| [状态变更] | [步骤] | [弹窗必填项 + 状态结果] |
| [Mock 开启] | [步骤] | [出现 §4 引导] |
| [Mock 生成] | [步骤] | [本地数据 + 联动] |
| [Mock 关闭] | Demo 配置关闭 | [仅成功提示，不弹 Mock] |
| [业务阻断] | [步骤] | Toast 阻断，不进入成功流 |

Demo 验证弹窗文案、必填项与 Mock 本地数据联动；不代表生产接口、并发或后端事务行为。

---

## 修订记录

| 日期 | 变更摘要 |
| :--- | :--- |
| [日期] | [本模块首次设计或变更摘要，不复制模板修订历史] |
