# [模块名]前端Demo版PRD·新增/编辑页

> **模板版本**：V2
> **更新时间**：[YYYY-MM-DD]
> **定位**：供 AI 生成新增/编辑页前端原型。字段见[《[对象名]（详细稿）》]([TSV路径])；业务规则见[《[模块名]主PRD》]([主PRD路径])（[规则章节引用]）。**本文件只写页面呈现，不重复字段含义与枚举。**
> **通用口径**：控件校验与错误展示见[《UI布局公共说明》](../UI布局公共说明.md)、[《原型开发说明》](../../../09-产品原型/原型开发说明.md)（表单页标准接线）；字段模板见[《6.强盛ERP的编码规则和通用字段规则》](../../../01-全局背景信息/6.强盛ERP的编码规则和通用字段规则.md)。对象特例须有已确认依据；与全局规范冲突时，按根AGENTS.md核对后同步；下表已列明的文案以本文件为准。

关联资料：[《[模块名]页面骨架》]([骨架路径])、[《[模块名]前端Demo版PRD_弹窗与Mock》]([弹窗PRD路径，无则删])。布局见骨架；**实现参照**（可选）：`09-产品原型/src/pages/[XxxFormPage].jsx`。

共享弹窗正文和共享Mock行为统一在《[模块名]前端Demo版PRD_弹窗与Mock》维护；本页只写入口、确认后的页面表现、本页数据覆盖与验收。没有独立弹窗文档时，指定一份页面PRD维护共享内容，其他页面引用，避免各写一套。

删除不适用的章节和示例行；实际文案直接填写，不只写「提示成功」或「报错」。未知内容写「待确认」。

---

## 1. 页面概述

| 项目 | 内容 |
| :--- | :--- |
| 页面名称 | [新增 / 草稿编辑页名称] |
| 页面目标 | [用业务语言写用户要完成什么，如录入向谁买、买什么、数量与价格] |
| 适用状态 | [新增页建草稿；哪些状态可进入编辑，引用主 PRD §6.4] |
| 骨架结构 | 页头（标题 + 操作按钮）→ [单头分组…] → [明细或其他区块] |

> 操作按钮固定在**页头右侧**，不使用底部固定操作栏。[不可进入编辑的状态与例外操作写明]。本页落地 **[功能编号]**；状态—操作矩阵见主 PRD [章节]。

| 维度 | 新增 | 编辑 |
| --- | --- | --- |
| 入口 | [菜单或按钮] | [列表／详情入口] |
| 单号 | [占位或生成时机，引用规则] | [只读规则及依据] |
| 特有字段或明细列 | [无则填无] | [与新增的差异] |

状态标签及标题见第二节；保存后的结果与跳转只在第六节维护。

> **本期边界**（按需）：
> - 页头按钮：[如 取消 / 保存 / 提交；勿与主 PRD 冲突]
> - 本页不展示的区块：[操作信息、关联单据等]
> - [模块特有：不做导入建单、不做某字段等]

---

## 2. 页头

| 元素 | 规格 |
| :--- | :--- |
| 标题（新增） | `[新增[模块名]]` |
| 标题（编辑） | `[编辑[模块名]]`；单号展示位置（页头标题 / 表单区只读，与骨架、原型一致） |
| 状态标签 | [新增不展示 / 编辑展示哪些 StatusBadge；引用主 PRD] |
| 操作区 | 从左到右：[按钮顺序] |

按钮行为见第六节；公共布局、加载与错误展示引用《UI布局公共说明》和原型规范。

---

## 3. 表单分区与字段

> 单头默认 **6 列栅格**，标签在上、控件在下；备注常占 **3 列**。控件 `type` 对齐原型 `FormControl` / `LineItemTable`（如 `disabled`、`date`、`select`、`textarea`、行内数值输入）。主数据按 Code-Name（`{编码} {名称}`）展示。**placeholder** 指控件无值时的占位文案；只读/禁用填 `—`。

区域顺序与 TSV 分组一致：[单据信息 → … → 商品明细]。

### 3.1 [单头分组名，如单据信息]

| 字段 | 控件 type | placeholder | 规格摘要 |
| :--- | :--- | :--- | :--- |
| [字段名] | `[type]` | `[占位文案或 —]` | [必填/只读/默认/联动；引用主 PRD 规则编号] |

[本组布局补充：如价税字段顺序、备注占列]

### 3.2 [其他单头分组，无则删]

| 字段 | 控件 type | placeholder | 规格摘要 |
| :--- | :--- | :--- | :--- |
| [字段名] | `[type]` | `[占位文案]` | [说明] |

### 3.3 [明细表分组名，无明细则整节删除]

独立表格区块，横向滚动（如适用）。

#### 3.3.1 表格操作

| 操作 | 位置 | 说明 |
| :--- | :--- | :--- |
| [添加明细/选品] | 工具条 | [弹窗或行内选品] |
| 删除 | 行内 | [是否至少保留一行；是否二次确认] |

**明细区错误条**（工具条上方，按需）：仅用于**跨行**提示（如明细为空、SKU 重复）；修正后即时消除。单行字段问题默认 Toast（见《原型开发说明》「表单页标准接线」）；若模块改为行内红字，在本节写明。

#### 3.3.2 列定义

| 列名 | 可编辑 | placeholder | 规格摘要 |
| :--- | :--- | :--- | :--- |
| [列名] | [只读/可编辑] | `[— 或请选择…]` | [字段模板如 QTY_INT、PRICE_4；计算列引用 R0x] |

#### 3.3.3 [选品弹窗名称]（本页配置）

无选品弹窗则删除本节。

| 配置项 | 本页取值 |
| :--- | :--- |
| 选品方式 | [单选/多选；是否搜索] |
| 商品/物料范围 | [启用范围；是否按供应商等过滤] |
| 确认后写入 | [带出字段；带价规则] |
| 更换行 | [删行重选 / 行内不可改编码等] |

#### 3.3.4 底部汇总

| 汇总项 | 展示 |
| :--- | :--- |
| [数量合计] | 与对应列右对齐 |
| [金额/价税合计等] | 千分位、小数位；与单头汇总区同步（如有） |

---

## 4. 校验规则

共用弹窗见[弹窗 PRD]([路径]) [章节]。下文 **保存** / **提交** 的定义须与主 PRD §7.7 或模块规则一致；**保存与提交的松紧不得在本文件另定一套业务门槛**。

### 4.1 校验分层总览

| 层级 | 本页行为 | 典型错误位置 |
| :--- | :--- | :--- |
| 录入限制 | [maxLength、禁小数、禁负数等] | 阻止输入或即时提示 |
| 失焦 | §4.2 格式与范围 | 字段下或 Toast；改值后即时清除 |
| 保存 | §4.3 | 单头内联；明细 Toast 或错误条 |
| 提交 | §4.4（在保存基础上加强） | 同上；通过后 [弹窗/直接提交] |
| 请求中 | 按钮 loading；防重复点击 | — |

### 4.2 录入与失焦

| 字段 | 模板 | 格式与范围 | 录入/失焦处理 | 典型提示 |
| :--- | :--- | :--- | :--- | :--- |
| [字段] | `[REMARK_500/QTY_INT/…]` | [范围] | [如何处理非法输入] | `[实际文案]` |

[补充：金额千分位展示录入不带；是否设数量/单价业务上限]

### 4.3 点击「保存」

| 校验对象 | 条件 | 错误位置 | 阻断 | 文案 |
| :--- | :--- | :--- | :---: | :--- |
| [单头字段] | [必填/格式] | 字段下方 | 是/否 | `{字段名}不能为空` |
| [明细项] | [条件] | Toast / 错误条 | 是/否 | `[实际文案]` |

校验通过后的处理见第六节，反馈文案见第七节。

### 4.4 点击「提交」

在 §4.3 全部通过基础上，追加：

| 校验对象 | 条件 | 错误位置 | 阻断 | 文案 |
| :--- | :--- | :--- | :---: | :--- |
| [加强项] | [如单价非空、主数据仍启用] | [位置] | 是 | `[文案]` |
| [零价/特殊确认] | [条件] | 确认框 | 确认后继续 | 见弹窗 PRD |

校验失败：聚焦首错（单头优先）；可用 Toast「请修正表单中的错误后再提交」。

### 4.5 价税计算与汇总（无价税则删整节）

依据主PRD **[R0x]**；公式、精度与尾差引用该规则，本节只写重算时机和页面表现。

| 计算项 | 规则来源 | 重算时机与展示 |
| :--- | :--- | :--- |
| [行/单头字段] | [主PRD规则编号] | [触发字段、刷新位置；无差异时沿用公共格式] |

| 异常输入 | 页面表现 |
| :--- | :--- |
| [空值/非数字] | [按 0 展示还是阻断；保存 vs 提交] |

### 4.6 错误展示与纠错

默认采用公共表单规则：单头错误同时标出并聚焦首错，改值后清除该项；失败保留已填内容。明细与跨行错误按原型规范处理，具体错误位置在第4.2～4.4节填写。

仅有差异时补下表，无差异则删除表格。

| 错误类型 | 本页表现 | 修正或继续方式 |
| --- | --- | --- |
| [错误] | [与公共规则不同之处] | [清除时机、保留数据及恢复入口] |

---

## 5. 联动规则

| 触发 | 效果 |
| :--- | :--- |
| [选供应商/改币别/选商品等] | [带出、清空、重算] |

无联动可删本章。

---

## 6. 操作行为

| 操作 | 校验或确认入口 | 成功后的状态、数据与页面表现 | 失败或取消后 |
| --- | --- | --- | --- |
| 保存 | §4.3 | [新增与编辑有差异时分别写；单号、状态、停留或跳转] | [保留输入、允许重试] |
| 提交 | §4.4；[弹窗文档及章节／无需确认] | [主PRD规则对应的结果、跳转] | [关闭弹窗后的停留与数据保留] |
| 取消／返回 | [未保存修改确认／无需确认] | [返回目标、筛选与页码恢复] | [继续编辑] |
| 进入编辑 | [主PRD可编辑条件] | [加载已有记录] | [加载失败／状态不符时的恢复入口] |

保存或提交请求中，当前按钮显示加载状态，其他提交类按钮禁用，避免重复操作；失败保留输入。确认弹窗正文在弹窗PRD维护，本页只引用；操作反馈文案见第七节。

---

## 7. 反馈文案

| 操作 | 反馈类型 | 文案 |
| :--- | :--- | :--- |
| 保存成功 | Toast | [实际文案] |
| 保存失败（通用/校验/网络） | Toast | [分场景文案] |
| 提交成功 | Toast | [实际文案] |
| 提交失败 | Toast + 字段提示 | [实际文案] |
| 加载失败 | 错误态 | [实际文案] |

字段错误文案在第4.2～4.4节维护，本表只写操作结果及加载异常；相同反馈可引用同一条文案。确认弹窗正文见弹窗PRD。

---

## 8. Mock 与验收

### 8.1 Mock 覆盖

- [可保存草稿的最小数据集]
- [编辑页专用：回写列、状态标签]
- [边界：零价、不可编辑状态等]

### 8.2 验收场景

| 场景 | 操作 | 预期 |
| :--- | :--- | :--- |
| [正常建单] | [步骤] | [预期] |
| [联动/无价保存/提交阻断/格式边界] | [步骤] | [预期] |

模拟的保存、状态变化和关联结果应可连续演示；Demo 验证不代表真实接口、并发或后端事务已验证。

---

## 修订记录

| 日期 | 变更摘要 |
| :--- | :--- |
| [日期] | [本模块首次设计或变更摘要，不复制模板修订历史] |
