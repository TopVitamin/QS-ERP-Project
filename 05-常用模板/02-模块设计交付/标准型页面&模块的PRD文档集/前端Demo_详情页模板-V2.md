# [模块名]前端Demo版PRD·详情页

> **模板版本**：V2
> **更新时间**：[YYYY-MM-DD]
> **定位**：供 AI 生成详情页前端原型。字段见[《[对象名]（详细稿）》]([TSV路径])；业务规则见[《[模块名]主PRD》]([主PRD路径])（[规则章节引用]）。**本文件只写页面呈现，不重复字段含义与枚举。**
> **通用口径**：布局与详情页接线见[《UI布局公共说明》](../UI布局公共说明.md)、[《原型开发说明》](../../../09-产品原型/原型开发说明.md)（详情页标准接线、StatusBadge）；字段模板见[《6.强盛ERP的编码规则和通用字段规则》](../../../01-全局背景信息/6.强盛ERP的编码规则和通用字段规则.md)。对象特例须有已确认依据；与全局规范冲突时，按根AGENTS.md核对后同步。

关联资料：[《[模块名]页面骨架》]([骨架路径])、[《[模块名]前端Demo版PRD_弹窗与Mock》]([弹窗PRD路径，无则删])。布局见骨架；**实现参照**（可选）：`09-产品原型/src/pages/[XxxDetailPage].jsx`。

共享弹窗正文和共享Mock行为统一在《[模块名]前端Demo版PRD_弹窗与Mock》维护；本页只写入口、确认后的页面表现、本页数据覆盖与验收。没有独立弹窗文档时，指定一份页面PRD维护共享内容，其他页面引用，避免各写一套。

删除不适用的章节和示例行；实际文案直接填写。未知内容写「待确认」。

---

## 1. 页面概述

| 项目 | 内容 |
| :--- | :--- |
| 页面名称 | [详情页名称] |
| 入口 | [列表点单号 / 其他入口] |
| 页面目标 | 只读查看[单据]全貌；按当前状态办理允许的操作 |
| 骨架结构 | 页头 → [单头分组…] → [明细] → [关联单据（条件）] → [终止信息（条件）] → [操作信息] |

本页落地 **[功能编号]**；状态—操作矩阵见主 PRD [章节]。

| 模式 | 说明 |
| :--- | :--- |
| 容器 | [独立页 / 抽屉等] |
| 返回 | `返回列表`（或实际文案），保留原查询条件与页码 |

> **本期边界**（按需）：
> - 页头状态标签：[如三枚 StatusBadge；引用主 PRD]
> - 关联单据/下游 Tab：[展示条件]
> - 终止信息：[业务状态条件]
> - 操作信息：[制单信息 / 操作日志 Tab 等]
> - 二次确认弹窗正文在[弹窗 PRD]([路径])维护，本文只写入口与结果表现
> - [模块特有边界，如 S08 仅查看、不做移动端等]
> - Demo 以桌面端为基准；明细、关联简表窄视口横向滚动

---

## 2. 页头

### 2.1 标题与状态

| 元素 | 规格 |
| :--- | :--- |
| 标题 | `[模块名]详情 · {单号}` |
| 状态标签 | [StatusBadge 列表；或写在标题区的方式] |
| 布局 | 单行：左侧标题 + 状态；右侧操作区（`DocumentDetailFrame`） |

列表状态列与详情页头状态展示方式差异见《原型开发说明》（列表语义色文字 vs 详情 StatusBadge）。

### 2.2 页头操作按钮

`返回列表` **始终排在最前**；其后按状态展示业务按钮。[编辑] 等主按钮由 `canEdit` / `renderHeaderActions` 提供时单独说明。

| 按钮 | [状态列 A] | [状态列 B] | … | 说明 |
| :--- | :---: | :---: | :---: | :--- |
| 返回列表 | ✅ | ✅ | … | 始终 |
| [业务按钮] | [✅/❌/条件✅] | [✅/❌/条件✅] | … | [引用主 PRD §6.4] |

条件说明表（按需）：

| 按钮 | 条件摘要 |
| :--- | :--- |
| [按钮] | [主 PRD 规则编号与简述] |

[按钮建议顺序]（`返回列表` 之后）：[与列表行内、骨架一致]。

**渲染纪律**：

- 条件不满足的按钮**直接不渲染**，不展示灰色 disabled。
- 样式层级：**[主操作]** 用 `primary`；**[次要/危险操作]** 用 `outline`；**返回列表** 固定最左。

二次确认入口与确认后的页面表现统一在第五节维护；弹窗正文引用弹窗PRD。

---

## 3. 信息分区与字段

> 只读 **6 列栅格**（默认），标签在上、值在下；备注常占 **3 列**。字段顺序与 TSV 分组一致。

**分区总览**：页头 → **3.1 …** → **3.2 …** → …

### 3.1 [单头分组名]

| 字段 | 展示说明 |
| :--- | :--- |
| [字段] | [格式；Code-Name；占列] |

[分行布局说明]

### 3.2 [其他单头分组，无则删]

### 3.3 [商品/明细表，无明细则删]

- 表格只读；数量、金额列**右对齐**；横向滚动。
- 一期是否分页：[不分页 / 每页 N 条]
- 价税列顺序（有价税时）：[含税单价 → 税率 → …]

| 列名（从左到右） | 说明 |
| :--- | :--- |
| [列] | [格式；回写列等] |

**回写列显隐**（按需）：[始终展示为 0 / 按状态隐藏列]

**底部汇总行**（按需）：

| 汇总项 | 展示 |
| :--- | :--- |
| 首列 | 文案 `合计` |
| [数量/金额合计] | 与对应列右对齐 |

### 3.4 [关联单据 / 下游 Tab，无则删]

**展示条件**：[如 已审核]

组件：`RelatedDocumentsCard` + `InnerTabs` `pill`；Tab 文案带数量。

| Tab | 列 | 排序 | 空态 |
| :--- | :--- | :--- | :--- |
| [下游单据] | [列清单] | [排序] | [空态文案] |

### 3.5 [终止信息，无则删]

**展示条件**：[已关闭 / 已取消等]

| 业务状态 | 字段 |
| :--- | :--- |
| [状态] | [字段列表] |

### 3.6 [操作信息，无则删]

胶囊 Tab：[制单信息] / [操作日志]

| Tab | 内容 |
| :--- | :--- |
| 制单信息 | [字段] |
| 操作日志 | [表格列；推导规则] |

---

## 4. 字段展示规范

空值、Code-Name、数字与日期格式、状态样式、关联简表均引用公共规范；不在此重抄。数量是否允许小数及金额精度以TSV、主PRD和已引用的字段规则为准。无模块差异时删除下表。

| 字段或区域 | 本页差异 | 依据 |
| --- | --- | --- |
| [字段／区域] | [特殊精度、长文本处理或展示方式] | [TSV／主PRD／已确认设计] |

---

## 5. 操作行为

| 操作 | 点击行为或弹窗入口 | 确认后页面表现 |
| --- | --- | --- |
| [编辑] | [跳转编辑页] | [不涉及确认则填不适用] |
| [下推／创建下游] | [跳转或弹窗文档及章节] | [刷新、跳转、关联区变化] |
| [状态变更类] | [弹窗文档及章节] | [刷新详情或返回列表；反馈引用第六节] |
| 返回列表 | [返回目标] | 保留原筛选与页码 |

没有独立弹窗PRD时，指定一份页面PRD维护共享确认文案，本页引用。

---

## 6. 反馈与页面状态

### 6.1 操作反馈（Toast）

| 操作 | 反馈类型 | 文案 |
| :--- | :--- | :--- |
| [成功] | Toast | [实际文案] |
| [业务阻断] | Toast | [如 R09 失败文案] |
| 状态已变更 | Toast | 操作失败，单据状态已变更，请刷新后重试 |

Mock 专属弹窗见弹窗 PRD。

### 6.2 加载与异常态

| 场景 | 页面表现 | 文案 / 操作 |
| :--- | :--- | :--- |
| 加载中 | 骨架屏 | — |
| 单据不存在 | 错误态 | [文案]；返回列表 |
| 加载失败 | 错误态 | 加载失败，请重试；重试 + 返回列表 |

---

## 7. Mock 与验收

### 7.1 Mock 覆盖清单

- [各主状态至少 1 单]
- [边界状态，如全部收足 / 已关闭 / 已取消]
- [关联 Tab 有数据 + 空态]
- [操作日志 / 终止信息边界]

### 7.2 验收场景

| 场景 | 操作 | 预期 |
| :--- | :--- | :--- |
| [正常路径] | [步骤] | [预期] |
| [边界] | [步骤] | [预期] |

模拟的保存、状态变化和关联结果应可连续演示；Demo 验证不代表真实接口、并发或后端事务已验证。

---

## 修订记录

| 日期 | 变更摘要 |
| :--- | :--- |
| [日期] | [本模块首次设计或变更摘要，不复制模板修订历史] |
